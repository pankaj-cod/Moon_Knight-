{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd moon_backend && npm install && npx prisma generate"
    },
    "deploy": {
        "startCommand": "cd moon_backend && npx prisma migrate deploy && node server.js",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}